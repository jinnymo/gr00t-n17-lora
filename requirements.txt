# Wrapper-side requirements only.
# NVIDIA Isaac-GR00T must be installed separately; see README.
#
# Pinned to the exact versions this wrapper has been tested against.
# Newer versions may work but are not verified.

peft==0.17.1
tyro==0.9.17

# Optional: enables paged_adamw_8bit when present.
# bitsandbytes==0.49.2
